#ifndef MEM_H
#include <locust/rt.mem.h>
#endif

/*
int8 mem_cmp(slice s1, slice s2) {
    while (s1.len-->0 && s2.len-->0 && (*(byte*)(s1.bytes++) == *(byte*)(s2.bytes++)));
    return *(byte*)s1.bytes-*(byte*)s2.bytes;
}
*/
